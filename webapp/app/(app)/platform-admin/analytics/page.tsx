import { PlatformAdminRoutePlaceholder } from "@/src/components/platform-admin/PlatformAdminRoutePlaceholder";

export default function PlatformAdminAnalyticsPage() {
    return (
        <PlatformAdminRoutePlaceholder
            eyebrow="Reserved for Story 2.11"
            title="Analytics"
            description="Platform analytics remains intentionally unavailable until the live metrics sources and deeper operational analysis stories are ready."
        />
    );
}
