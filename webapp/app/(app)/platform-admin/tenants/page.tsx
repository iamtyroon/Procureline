import { PlatformAdminRoutePlaceholder } from "@/src/components/platform-admin/PlatformAdminRoutePlaceholder";

export default function PlatformAdminTenantsPage() {
    return (
        <PlatformAdminRoutePlaceholder
            eyebrow="Reserved for Story 2.3"
            title="All Tenants"
            description="This platform-admin destination stays live so the dashboard can link honestly to the future tenant roster and provisioning workflow."
        />
    );
}
